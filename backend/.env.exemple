# Database Postgres :)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/database
SPRING_DATASOURCE_USERNAME=changeme
SPRING_DATASOURCE_PASSWORD=changeme

# Comma separated list of allowed origins to access the API
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:4173

# Cloudinary API
CLOUDINARY_CLOUD_NAME=changeme
CLOUDINARY_API_KEY=changeme
CLOUDINARY_API_SECRET=changeme

# Spring mail
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=changeme@cookit.com
SPRING_MAIL_PASSWORD=changeme

# JWT
JWT_SECRET=changeme